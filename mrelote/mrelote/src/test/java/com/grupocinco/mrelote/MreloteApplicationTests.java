package com.grupocinco.mrelote;

import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@Disabled("Requires live Supabase credentials (SUPABASE_* env vars). Enable once credentials are configured.")
@SpringBootTest
class MreloteApplicationTests {

	@Test
	void contextLoads() {
	}

}
